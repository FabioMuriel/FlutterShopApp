import 'package:flutter/material.dart';

class CategorieTitle extends StatelessWidget {
  
  const CategorieTitle({super.key});

  @override
  Widget build(BuildContext context) => const Text('Categories', style: TextStyle(fontFamily: 'VarelaRound', fontSize: 18, fontWeight: FontWeight.bold));
}

